<p>Ciao <%= @resource.email %>!</p>

<p>Il tuo account è stato bloccato a causa di un eccessivo numero di tentativiv di accesso non andati a buon fine.</p>

<p>Clicca sul link seguente per sbloccare il tuo account:</p>

<p><%= link_to 'Sblocca il mio account', unlock_url(@resource, unlock_token: @token) %></p>
